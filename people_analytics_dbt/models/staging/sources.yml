version: 2

sources:
  - name: hr_system
    description: "Raw Workday HCM extracts generated by the Python data generator."
    schema: main
    tables:
      - name: raw_workday_employees
        description: "Current-state employee extract; one row per employee."
      - name: raw_workday_transactions
        description: "Employee lifecycle and job-change event log; one row per HR transaction."
